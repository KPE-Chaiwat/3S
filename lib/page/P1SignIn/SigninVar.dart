import '../../model/model.dart';

class DataSignin {
  static DATABODY userId = DATABODY(str: '', inte: 0, bools: false);
  static DATABODY passWord = DATABODY(str: '', inte: 0, bools: false);
  static DATABODY rememberUserPass = DATABODY(str: '', inte: 0, bools: false);
}
